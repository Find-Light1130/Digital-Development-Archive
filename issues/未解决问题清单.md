# 未解决问题清单

> 更新日期：2026-08-02
> 说明：本文档汇总当前系统中**尚未解决**的问题与待办事项。U-01 ~ U-10、U-12 ~ U-14 已于 2026-08-01 全部修复并通过 `test/backend_tests.py`、`test/frontend_tests.js` 验证；U-15 ~ U-19 已于 2026-08-02 修复（见下文）；U-20 ~ U-26（全盘检修）已于 2026-08-02 修复并通过 69 项后端 / 28 项前端测试；U-11（用户登录/角色权限系统）为用户明确暂缓项。已修复问题见 `修复与安全升级计划.md`（其中问题 1-7 及非登录类安全加固已完成）与 `修复计划.md`。
>
> 状态说明：`已解决` = 已实施并通过验证；`暂缓` = 用户明确本轮不做；`待确认` = 需用户确认需求后实施。

---

## 一、前端体验类

### U-01 成绩对照表固定列文字重叠（sticky 列背景半透明）
- **状态**：已解决
- **位置**：`frontend/src/components/ScoreChart.vue`（`.corner` / `.row-label`）；`frontend/src/views/TeacherDashboard.vue` 同构样式
- **说明**：sticky 列背景采用「`--glass-bg` 透明白 + 不透明 `--glass-bg-solid` 底色」双层渐变（与卡片底色一致、完全遮住后方内容），横向滚动不再透出重叠；hover 时叠加 accent 透明层。

### U-02 折线图 markLine 标签出现虚影
- **状态**：已解决
- **位置**：`frontend/src/components/ScoreChart.vue`（原 `series.markLine.label.position`）
- **说明**：原 `markLine` 标签位置改为 `start` 修复虚影；后续 1B 迭代已整段移除 markLine，改为 `axisLabel.color` 函数（含「期中」标琥珀 `#fbbf24`、含「期末」标红 `#f87171`、其余默认灰），从根源避免标签渲染问题。

### U-03 滚动条样式未跨浏览器统一
- **状态**：已解决
- **位置**：`frontend/src/components/ScoreChart.vue`、`frontend/src/views/TeacherDashboard.vue`（`.table-scroll`）
- **说明**：已补充 `scrollbar-width: thin; scrollbar-color: var(--text-label) transparent;`，Firefox 与 WebKit 外观统一。

### U-04 教师端雷达图 emphasis 样式与配色不一致
- **状态**：已解决
- **位置**：`frontend/src/views/TeacherDashboard.vue`、`frontend/src/components/RadarChart.vue`（`radarOption` 约 L286）
- **说明**：已移除 `emphasis.lineStyle`（选中后线正常消失）；教师端与学生端统一使用固定 `#34d399`，浅色主题下两端颜色一致。

### U-05 表格行高亮策略待确认
- **状态**：已解决（已与用户确认，改为仅选中行高亮）
- **位置**：`frontend/src/App.vue`（`.glass-table .el-table__body tr:hover` / `tr.current-row`）
- **说明**：已使用 `el-table` 的 `highlight-current-row`，当前行高亮为 `rgba(var(--accent-rgb), 0.10)`；hover 高亮已移除（`--el-table-row-hover-bg-color: transparent`）。已在教师端关注学生列表、管理端各年级详情启用。

### U-06 学期下拉未按学年分组显示
- **状态**：已解决（已与用户确认，按学年分组显示）
- **位置**：`frontend/src/views/TeacherDashboard.vue`、`frontend/src/views/StudentDashboard.vue`、`frontend/src/views/StudentDetail.vue`（学期 `<el-select>`）
- **说明**：新增 `frontend/src/utils/semesters.js`（`semesterGroups`），学期下拉按「YYYY-YYYY 学年」`el-option-group` 分组展示（如「2025-2026 学年」下含初一上 / 初一下）。

---

## 二、后端 / 数据类

### U-07 部分接口前端未接入（死代码）
- **状态**：已解决（移除死代码）
- **位置**：`backend/routes/student_api.py`（`/api/student/report`、`/api/student/suggestions`、`/api/student/activities`）
- **说明**：已移除三个无前端调用的接口、`api.js` 中未使用的 `getStudentSuggestions`，以及 `analysis.py` 中仅被 `/report` 使用的 `compute_knowledge_mastery`；`test/backend_tests.py` 已断言三个接口返回 404。建议已随 `/profile` 返回，无需独立接口。

### U-08 POST 提交无幂等控制
- **状态**：已解决
- **位置**：`backend/routes/student_api.py`（`/api/student/emotion`）、`backend/routes/teacher_api.py`（`/api/teacher/student_event`）
- **说明**：`/emotion` 按 `(student_id, date)` upsert（重复提交更新 `emotion_level`）；`/student_event` 按 `(student_id, type, date)` upsert（同类型同日更新 `hours`，不同类型同日可并存）。`test/backend_tests.py` 已覆盖幂等与并存用例。

### U-09 成绩接口无分页
- **状态**：已解决
- **位置**：`backend/routes/student_api.py`（`/api/student/scores`）
- **说明**：已支持可选 `limit`（1-1000）与 `offset` 参数，并按 `(date, id)` 稳定排序；不传参数时行为与原来一致。`test/backend_tests.py` 已覆盖 limit/offset 与非法参数（422）。

---

## 三、安全类

### U-10 CSP 未覆盖 SPA 页面
- **状态**：已解决
- **位置**：`frontend/index.html`（CSP `<meta>`）、`backend/app.py`（`Content-Security-Policy` 响应头）
- **说明**：在 `index.html` 增加 CSP `<meta>`（`default-src 'self'` + 宽松 `script/style-src 'unsafe-inline'`、`connect-src ws: wss: http: https:`），覆盖 SPA 页面本身且不阻碍 Vite HMR/构建产物；后端 API 响应头保持不变。生产部署如需更严格策略，仍建议在反向代理层配置。

### U-11 用户登录 / 角色权限系统（暂缓）
- **状态**：暂缓
- **说明**：用户已明确本轮不实施。后续需产出详细方案，含：本地 vs 云端会话存储权衡、JWT vs 服务端 Session、角色模型（学生/教师/管理）与接口鉴权改造范围。
- **建议**：作为独立任务立项，参照 `修复与安全升级计划.md` 中「决策 2」的说明。

---

## 四、全盘检修（2026-08-02 大检修）

> 对后端/前端/数据·测试·文档三路并行审计后的集中整改，全部完成并通过 `test/backend_tests.py`（69 项）与 `test/frontend_tests.js`（28 项）验证。

### U-20 前端可见缺陷（Tier1）
- **状态**：已解决
- **位置**：`frontend/src/components/QualityChart.vue`、`frontend/src/views/TeacherDashboard.vue`、`StudentDashboard.vue`、`StudentDetail.vue`、`AdminDashboard.vue`
- **说明**：① QualityChart 初始无选中科目导致空白 → 挂载/`watch(subjectList)`/深度 watch 三重自动选中首个科目，学期失效自动重置；② 教师端趋势图科目陈旧 → 班级/学期切换时重置 `teacherTrendSubject`；③ StudentDashboard 学期 watch 与 select 事件双重请求 → 移除 watch 改用 `@change`；④ 三视图加 `requestSeq` 令牌丢弃过期响应；⑤ admin 空库除零 500 → 空库守卫返回空结构。

### U-21 错误契约不统一（200+{error} vs HTTPException）
- **状态**：已解决
- **位置**：`backend/routes/student_api.py`、`teacher_api.py`、`admin_api.py`；前端各视图
- **说明**：全部 GET 改为 `HTTPException(404, "Student not found"/"Class not found")`；`student_id` 一律 `Query/Path(..., gt=0)`（违规 422）；`class_name` 不合法 400。前端移除 `.data.error` 分支，按 `status` 提示。

### U-22 输入校验缺口
- **状态**：已解决
- **位置**：`backend/routes/teacher_api.py`、`student_api.py`
- **说明**：`add_student_event` 拒 NaN（`math.isfinite`）、拒 value≤0 / >24、拒未来日期；`/emotion` 拒未来日期；活动/情绪写入后失效 `indices` 缓存。行为事件按 `constants.semester_from_date` 自动推导并写入 `Activity.semester`（超学业日历 400）。

### U-23 逻辑重复 / 阈值双源
- **状态**：已解决
- **位置**：`backend/constants.py`（新建）、`backend/ai_modules/analysis.py`、`data/raw_data_gen.py`、`backend/routes/admin_api.py`、`backend/routes/teacher_api.py`
- **说明**：9 档等级阈值与 `grade_for()`、学期顺序、日期→学期映射收敛到 `constants.py`；`_batch_growth_indices`（admin_api）删除，改共用 `batch_growth_profiles(ids, db, light=True)`（analysis.py 抽出 `_build_aspects` 单源）。

### U-24 缓存陈旧与各自实现
- **状态**：已解决
- **位置**：`backend/cache.py`（新建）、`backend/routes/admin_api.py`
- **说明**：统一为线程安全 TTL 缓存（300s，key=`indices`），写操作后 `invalidate`；`subject_mastery` 不再走旧缓存。

### U-25 管理端全表加载与慢查询
- **状态**：已解决
- **位置**：`backend/routes/admin_api.py`、`backend/models.py`
- **说明**：`/subject_mastery` 改 SQL `func.avg(score*100.0/max_score)` GROUP BY 聚合（不再加载全部成绩行）；新增索引 `ix_students_class`、`ix_scores_student_semester`、`ix_quality_student_semester`，`school.db` 已重建。

### U-26 脚本与工具健壮性
- **状态**：已解决
- **位置**：`start.ps1`、`start.sh`、`test/perf_check.py`、`test/verify.py`、`test/check.py`
- **说明**：一键脚本后端改绑 `127.0.0.1`、固定仓库根目录启动、60s 就绪超时（失败自动清理后端进程），`start.sh` 用 trap 保证退出时清理孤儿进程；`perf_check.py` 移除硬编码绝对路径；`verify.py`/`check.py`/`perf_check.py` 改为真检查（断言失败非零退出码）。

---

## 五、构建与工程类（U-12 ~ U-19）

### U-12 前端主包体积偏大（>1MB）
- **状态**：已解决
- **位置**：`frontend/src/utils/echarts.js`（按需注册）、`frontend/vite.config.js`（`manualChunks`）
- **说明**：改为 `echarts/core` 按需引入（仅注册 `LineChart`/`BarChart`/`RadarChart` + `Grid`/`Tooltip`/`Legend`/`Radar` 组件与 `CanvasRenderer`），并在 `vite.config.js` 用 `manualChunks` 拆分 `vue-vendor` / `element-plus` / `echarts`。构建后 echarts 独立 chunk 约 522KB（gzip 176KB），无 CJS 弃用与 >500kB 告警。

### U-13 Vite CJS Node API 弃用警告
- **状态**：已解决
- **位置**：`frontend/package.json`（`"type": "module"`）
- **说明**：为 `package.json` 增加 `"type": "module"`，Vite 以 ESM 方式加载配置，构建输出不再出现 CJS 弃用警告。

### U-14 文档/测试文件缺失
- **状态**：已解决
- **位置**：`docs/`、`test/`
- **说明**：已补写 `docs/demo_plan.md`（演示脚本）；`test/` 新增后端接口自动化测试 `backend_tests.py`（33 项用例，含幂等/分页/安全/死代码/综合素质摘要断言）与前端构建冒烟测试 `frontend_tests.js`（构建产物 + 源码静态检查）。原有 `check.py`/`verify.py`/`perf_check.py` 保留。

### U-15 成绩对照表单元格大小不统一、横向滚动不顺滑
- **状态**：已解决
- **位置**：`frontend/src/components/ScoreChart.vue`、`frontend/src/views/TeacherDashboard.vue`（`.score-grid` / `.table-scroll`）
- **说明**：两处成绩对照表统一为 `table-layout: fixed` + `<colgroup>` 显式列宽（`.col-corner` / `.col-data` 均 76px），所有单元格等宽、不再被内容撑开；`.table-scroll` 增加 `scroll-snap-type: x mandatory` + `scroll-padding-left: 76px`，表头/单元格 `scroll-snap-align: start`，横向滚动时自动对齐到整列。

### U-16 音体美信改为多维 ABC 等级评估（新增需求）
- **状态**：已解决
- **位置**：`backend/models.py`（`QualityScore` 表）、`data/raw_data_gen.py`、`backend/routes/student_api.py`、`backend/routes/teacher_api.py`、`backend/routes/admin_api.py`、`frontend/src/components/QualityChart.vue`
- **说明**：音乐/体育/美术/信息技术不再计入 scores，改由新表 `quality_scores`（student_id/subject/semester/dimension/score/grade）存储 4 维度×4 科目×6 学期成绩，数据已重建（67200 条）。等级采用 9 档（A+≥93 / A≥87 / A-≥80 / B+≥73 / B≥66 / B-≥59 / C+≥52 / C≥45 / C-<45），阈值在 `raw_data_gen.py` 与 `QualityChart.vue` 双端一致。新增接口：`GET /api/student/quality`、`GET /api/teacher/class/quality`（含班级分布）、`GET /api/admin/subject_mastery`。前端新增 `QualityChart.vue`（雷达图 + ABC 等级表 + 班级分布 tooltip），已接入学生端（个人/详情）与教师端（班级）。

### U-17 实践活动显示"暂无活动记录"（数据存在却未展示）
- **状态**：已解决
- **位置**：`frontend/src/views/StudentDashboard.vue`、`frontend/src/views/StudentDetail.vue`
- **说明**：根因是后端返回 `summary.semester_stats`（snake_case），前端误读 `semesterStats`（camelCase）导致 undefined。两处 `loadData` 均改为对 summary 做归一化：`{ semesterStats: sumRes.data?.semester_stats || [], ...sumRes.data }`，活动记录（3405 条）正常展示。

### U-18 教师端掌握率条形图混入音体美信、缺乏素质维度可视化
- **状态**：已解决
- **位置**：`frontend/src/views/TeacherDashboard.vue`
- **说明**：掌握率条形图仅保留九大学科（`masteryOption` 显式过滤 `QUALITY_SUBJECTS = ['音乐','体育','美术','信息技术']`）；新增「音体美信素质评估」卡片（复用 `QualityChart.vue`），展示班级各科维度均分与等级分布，随学期联动加载。

### U-19 管理端缺少各学科平均掌握率表
- **状态**：已解决
- **位置**：`frontend/src/views/AdminDashboard.vue`、`backend/routes/admin_api.py`
- **说明**：新增 `GET /api/admin/subject_mastery`（返回 `{grades, subjects, rows:[{grade, subject, mastery}]}`，仅九大学科，音体美信不计入）；管理端新增「各学科平均掌握率」表格（行=科目、列=年级，≥80 绿 / ≥70 黄 / 其余红）。

---

## 六、录入与交互迭代（2026-08-07）

> 本轮新增写入型业务闭环与下钻/折叠交互，全部完成并通过后端 199 项、前端 80 项测试验证。

### U-27 成绩/获奖仅有查询、无写入闭环
- **状态**：已解决
- **位置**：`backend/routes/teacher_api.py`、`backend/constants.py`、`frontend/src/views/TeacherDashboard.vue`、`frontend/src/utils/api.js`
- **说明**：新增写入接口 `POST /api/teacher/scores`（单条，同键 `(student_id, subject, exam_type, date)` 幂等覆盖）、`POST /api/teacher/scores/batch`（整班批量，任一非法整批拒绝且不落库）、`POST /api/teacher/scores/delete`（删除更正）、`POST /api/teacher/award`、`POST /api/teacher/award/delete`、`GET /api/teacher/class/students`（班级花名册 + 当前学期科目/满分）。科目集与满分收敛到 `backend/constants.py`（`SUBJECTS_BY_SEMESTER` / `MAX_SCORES` / `EXAM_TYPES` / `AWARD_LEVELS`），生成脚本与录入校验同源。前端教师端新增「成绩录入」卡片（单条 + 班级批量网格，实时均分/完成度预览）与「获奖登记」卡片（含撤销）。

### U-28 情绪记录无标签、无日历视图
- **状态**：已解决
- **位置**：`backend/models.py`、`backend/app.py`、`backend/routes/student_api.py`、`frontend/src/views/StudentDashboard.vue`、`frontend/src/components/MoodCalendar.vue`（新增）
- **说明**：`emotions` 表新增可空 `tags` 列（启动时幂等 ALTER 迁移，无需重建库）；`/api/student/emotion` 支持可选 `tags`（至多 3 个，白名单：开心/平静/焦虑/疲惫/生气/悲伤），`/api/student/emotions` 返回标签数组。前端新增「情绪日历」卡片（月历热力图，按 1-3 级着色，点击任意一天记录/修改当天心情与标签）与「连续记录 N 天」徽章。

### U-29 分布图无法下钻、卡片不可折叠
- **状态**：已解决
- **位置**：`backend/routes/teacher_api.py`、`backend/routes/admin_api.py`、`frontend/src/components/DistributionChart.vue`、`frontend/src/views/TeacherDashboard.vue`、`frontend/src/views/AdminDashboard.vue`
- **说明**：分布图柱子可点击（`@bucket-click`），教师端 `GET /api/teacher/class/distribution/students`、管理端 `GET /api/admin/distribution/students` 返回落入指定桶标签的学生明细，前端弹窗展示并可直达学生档案（管理端含班级列）。教师端各卡片头部新增折叠按钮，折叠状态存 localStorage。

### U-30 数据生成脚本与校验逻辑科目重复
- **状态**：已解决
- **位置**：`backend/constants.py`、`data/raw_data_gen.py`、`backend/routes/teacher_api.py`
- **说明**：每学期科目集与满分从 `raw_data_gen.py` / `teacher_api.py` 收敛到 `backend/constants.py` 单一事实来源；`raw_data_gen.py` 与录入校验均引用 `SUBJECTS_BY_SEMESTER` / `MAX_SCORES`，避免改一处漏一处。

---

## 七、回归验证注意事项

- 考试标签唯一化逻辑改动后，需在「教师端趋势 + 对照表」「学生端折线图 + 对照表」两处回归：全部学期与单学期场景下，`期中`/`期末`/`X月月考` 是否出现重复或缺失。
- 浅色主题下需回归：ECharts tooltip 背景/文字、下拉悬停高亮、sticky 列背景三处视觉效果。
- 若日后修改 `EXAM_WEEKS` 或增加考试场次，需重新验证 `teacherTrendOption.axisLabel.interval: 'auto'` 下标签是否拥挤。
- 音体美信素质评估改动后需回归：新增/重建数据库后 `quality_scores` 是否生成完整（67200 条）、学生端/教师端素质卡片随学期切换是否刷新、ABC 等级阈值与 `QualityChart.vue` 是否一致。
- 若修改 `QUALITY_DIMENSIONS`（`data/raw_data_gen.py`），需同步 `backend/routes/teacher_api.py` 的维度输出与前端 `QualityChart.vue` 的雷达图指标，避免维度缺失。`class_name` 校验格式为 `初/高 + 一二三 + 数字 + 班`（如 `初一1班`），测试中不存在的班级须用合法格式。
